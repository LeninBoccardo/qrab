// Vitest setup — runs once before each test file. Registers the
// jest-dom matchers (`toBeInTheDocument`, `toHaveClass`, etc.) so
// component tests can assert DOM state idiomatically.

import "@testing-library/jest-dom/vitest";
